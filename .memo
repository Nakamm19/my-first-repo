; scss

; 1 @use "../global" as *;
; アスタリスクの意味。掛け算？

; 2 globalの＠useを使わないのはなぜ。

; mixin       @contentとは？なぜ使う。

; HTML　CDNとは？

////授業メモ　ＩＤ name Test　ｐｓいつもの
; テンプレート階層

; Preesおさらい：CMSであるWordPrees
; 左から右に階層順番をした。index.phpを真っ白にする。
; ローカル

; VSコードのテーマズの中にある。
; 　・自分で作りたくなったら、オリジナルフォルダをつくり、index.phpとstyle.cssを作る。
; 　・headerとfooterのphpをつくって、HTMLのheadとbodyを入れる。
; 　・関数（get_headerとget_foterをつかう）

; 　・必須
; 　　headタグの中に、<?php wp_head(); ?>
;     bodyタグの中に、<?php wp_footer(): ?>を入れる

; 　　404.phpで「このページは存在しません」と記述。

！　画像も、phpで記述しないといけない。
　　zeroplusの確認しながらfunction.phpも操作しよう。

　　スクリーンショットも作れる。テーマのやつ。

　読み込むファイル（HTML・CSS・JSなど）funcsitons.phpを使う！！
　　functionは、mix inと同じ機能。繰り返し使う処理は、function関数を使う。
　　関数名は、その処理名が分かるものが良い。（ｓがあれば、スタイルシート複数なのかなと思う）
　　・function 関数名｛ブロック｝で実行。
　　
　　add_actionは、[”なにをするタイミングか？”　”関数名がそれをする” ]　引数で記述

    wp.content/～　が最初の相対パス。　get_template_direction　urlという関数で、略せる。その後は、読み込みたいファイル。
    基本は、get_template_direction で使う。

　　・関数：wp_enqueue_script　　
    依存関係とは？、まえに付けたの名前の後に、その記述入力してくれるという意味。

　　jsのライブラリを使うと、簡単にできる。（ライブラリによって、j-qary（例：slick）とjavaをつかったものある。）
　　turuを入れる（後で読み込む）。デフォルトはfales(すぐに読み込まれる。)


------ここまでをfuncsitons.phpにいれる。

word Preesの投稿について。

投稿は、postと書いてある。


他のWordpressの機



HTMLとCSSはDOMを創る。そのDOMで、JSを操作する。
scriptとは？　defer属性は、後から読み込むことになる。


4/3 memo

セキュリティの強化！　URLを変える
プラグインを使う。（例：site geard・サイトガード、　）
プラグインを入れると、プラグインの使い方を一回使って覚えましょう。

post（投稿） デフォルト　は、wordPresは１つのみ。
もし、投稿ページを２つ以上に増やす場合は、１.プラグインで増やす。
２.コードで設定をする。
カスタム投稿タイプというコードでで。

覚えること
①カスタム投稿
②カスタムフィールド
　お客さんが簡単にできるように、投稿ページを作成することができる。
　プラグインで作る。（smart カスタムフィールド。アドバンスカスタムフィールド。カスタムフィールド（Git Hubでできる。））
　スマートカスタムフィールドを作って、インストール。
　phpで記述。SCF~~~~で記述。
　HTMLふって、CSSで装飾。

③タクソノミーターム
　カスムタクソノミーで、カスタム投稿の中に、カテゴリーを作り個別の投稿をページを作成することができる。
④デバックで、データがどこまで取得や処理ができているかを確認をする。
var_dump()でPHPの中務の記述を確認することができる。



; ！！！！プラグインの種類。一度はさわってください。！！！！！！！！！！！！！！！！！！！！！
・１．ALL in-One WP 　→バックアップ取っていれば、何かあった時に戻せたりする。ローカル作成して公開するときにエクスポート・インポートしやすくなる。
　　　　　　　　　※注意点：インポートするときに上書きしてしまう。お客さんが何かページを作っていたら。
　　　　　　　　　　　　　　その場合は、ソースコードだけアップロードするといい。
　　　　　　　　　　　　　　ファイルをアップロードする。サーバーを繋げて、修正したいファイルをとってきて、修正してそこだけ上書きする事ができました。
　　　　　　　　　　　　　　（FileZillaを使って、サーバーを繋げる。）



修正で一番気を付けておきたいこと。
テーマファイルエディターで、ソースコードをいじるのはダメ。保存が出来ないから。


// 4/24　メモ

プラグインとは。

プラグインは少ない方が良い。サーバー攻撃を受けにくくなるから。

; ！！！プラグインの種類
プラグインいっぱい出て、基本的には触ったことがある物で触る。
探す時のコツは、他の人に聞いてみたりWebで探したりする。
　　　　　　　　使ってほしいプラグインの指定がたまにあったりする。

・２．Contact Form 7
コンタクトフォームセブン（お問い合わせページとサンクスページ）
　固定ページは何個も作れる。　お問い合わせページを作れる。
  問い合わせページとサンクスのページをつくることができる。
　確認ページは作れない。

・３． Multi-Step Forms
コンタクトフォームマルチページ（確認ページのショートコード）
　　２．のプラグインでは作れない確認ページをつくることができる。

HubSpotというサービスがある。ショートコードとかがある。


画像：メガ超えると良くない。
・画像を軽量化するプラグインがある。
・セキュリティ強化のために、初期URLから変えることができるものがある。


☑項目について。

☑デザインの再現性
・デザインのアニメーションは相違が無いか。
・指示書と相違ないのか確認をする。（参考サイトとかがあれば、送ってください。）
・出来たら、先に実装をみてもらう。


☑機能・コード面
ローカルでお問い合わせホームをメールに送る。

ローカルのツールを選択。→メールpitを押す。→お問い合わせをここに届いていたらOK!

スパムについて。（メールサーバーについて）
・Wordpressは、お問い合わせホームでメール送信はなんでも送れる。（メアドを弄れる）

4.・プラグインを使う。　！！WP　Maik SMTP（使い方は慣れたら、簡単。）
・送り先が、WordPressから、契約したメールサーバーで送ることが出来る。ちゃんとしたメールサーバーであれば、迷惑メールに送られない。
・メールサーブとはまた違う。


☑設定面
インデックスとは。グーグルが審査したときの検索ワードを設定する。
noindexというがあり、検索結果に表示されないようにする。HTMLタグに記述。
（作りかけの時に使う。公開する時に外す。実務の時はとくに。　　　　　・Web試験のときにこれを入れておく。）

basic認証（公開前に認証を掛けて、パスワードがないと見られない状態にする。そして、パスワードがお客さんにパスワードを送って確認してもらう。）
プラグインで実装出来る。セキュリティ上出来る。

OGPは、クリック率が上がる。SNSで宣伝が出来る。
・OGPチェッカーがある。

SSL化
・納品方法で変わる。
ソースコードだけだったら、いらない。
サイト公開までだったら、世の中。

ファビコン設定ができているか。

☑ソフトスキル面
新規ユーザーを作って、編集者や管理者などの設定をする。
権限ごとにできることが変わる。ユーザ名・パスワードをそれぞれで分けられる。
（試験のときは、権限の設定を管理者用・投稿者用）で分ける。
カスタムフィールドの設定（クライアント側で記事投稿出来るか。お客さんができるかどうか。）


////授業メモ
テンプレート階層

Preesおさらい：CMSであるWordPrees
左から右に階層順番をした。index.phpを真っ白にする。
ローカル

VSコードのテーマズの中にある。
　・自分で作りたくなったら、オリジナルフォルダをつくり、index.phpとstyle.cssを作る。
　・headerとfooterのphpをつくって、HTMLのheadとbodyを入れる。
　・関数（get_headerとget_foterをつかう）

　・必須
　　headタグの中に、<?php wp_head(); ?>
    bodyタグの中に、<?php wp_footer(): ?>を入れる

　　404.phpで「このページは存在しません」と記述。

　　スクリーンショットも作れる。テーマのやつ。

　読み込むファイル（HTML・CSS・JSなど）funcsitons.phpを使う！！
　　functionは、mix inと同じ機能。繰り返し使う処理は、function関数を使う。
　　関数名は、その処理名が分かるものが良い。（ｓがあれば、スタイルシート複数なのかなと思う）
　　・function 関数名｛ブロック｝で実行。
　　
　　add_actionは、[”なにをするタイミングか？”　”関数名がそれをする” ]　引数で記述

    wp.content/～　が最初の相対パス。　get_template_direction　urlという関数で、略せる。その後は、読み込みたいファイル。
    基本は、get_template_direction で使う。

　　・関数：wp_enqueue_script　　
    依存関係とは？、まえに付けたの名前の後に、その記述入力してくれるという意味。

　　jsのライブラリを使うと、簡単にできる。（ライブラリによって、j-qary（例：slick）とjavaをつかったものある。）
　　turuを入れる（後で読み込む）。デフォルトはfales(すぐに読み込まれる。)


------ここまでをfuncsitons.phpにいれる。

word Preesの投稿について。

投稿は、postと書いてある。


他のWordpressの機



HTMLとCSSはDOMを創る。そのDOMで、JSを操作する。
scriptとは？　defer属性は、後から読み込むことになる。


5/23

; 実務試験について。
; 試験前のミーティングで櫻井さんと調整。簡単な説明。

コーディングの必須項目はぬけがないように。（最終確認で、リンクが切れないように）

pictureタグとsourceタグ・・・スマホとパソコンで違う画像で使い分けられる。
　　　　　　　　　　　　　　　divタグで囲まなくてよさそうだったら、pictureタグで大丈夫。

altの中身は空でも良い。（全く意味のないの装飾の時だけ。）

・キーボード操作で出来るようにする。
divで実装すると、キーボード操作はできない。btnやaタグを使う。

・jQaly swiper slickなどの　ライブラリを使用する。 

・CSSで、text-transformで、HTMLの小文字を大文字にする。HTMLは、最初だけ大文字。

・電話番号をマークupする。：aタグで、tel：””　＝　を入れると電話を都バス。（基本した方がいい。）

・スムーズスクロールのアニメーションをしているか。行きたいページをスクロールする感じでいく。

いｍｇタグは、必要不可欠な画像。
意味のない画像などの装飾は、できるだけＣＳＳでバックグラウンドイメージ

・エスケープをする。システムをつくるときはとくに意識する。サーバー乗っ取られないようにあえて、JSですることがあるらしい。
知識として、知っとくだけでいい。

・３桁になると、％で、表示とか。display行う。
・ハンバーガーメニューを開いたときに、後ろの背景は固定してスクロール出来ないようにする。
・スマホは、長押しがhover扱いになる。それをかえることができる。
・Retina対応を使う。解像度が高すぎると粗くみえる。2倍の画像を使って、読み込ませる。（使うとしてもメインビジュアルなど）
・ｖｗは使いやすい。
・ＨＴＭＬにtimeタグを使用する。timeタグの中に、datetime属性を入れてみる。
・CSSに、imgには、loading=lazyを入れる。画像を後から読み込む。表示されていない画像は読み込まれていないことで、サイト表示。
ただし、ファーストビューには入れない！
・レイアウトシフト対策を実施ている。スケルトン要素を実施する。divタグで要素の大きさ作る。
・画像は、webp形式で、画像を使う。（画像が軽くなる。）　
・HTML内にCSSは記述しない。
・エラーメッセジーで、拡張機能はJS無視して行う。

・フォームは、labelとinputで入れる。
必須項目や未記入を入れる。動作確認をする。（双方にする）
・フォームにのせたときにカーソルがのっているか。

・サイト表示・表示スピード。
クローム。サファリでチェックする。動作テストは、
・httpじゃなくて、htppsでサーバーを開く。httpせず、リダイレクトしてhttpsでする。（基本自動でできる）
・10pageSppe +insightsの指定。スコアを図って、速度が分かる。
・１メガ超えると圧縮した方が良い。でも基本圧縮しない方が良い。
・WebMnituite

・WordPressのプラグインで、セキュリティ対策強化。、例えば、SiteGuardwoirerunado
.納品していないプラグインは、消す。あとはデフォルトのテーマは消す。
・画像サイズは不要なサイズで出力されないようにプラグインで調整。
・仕様にあわせて、タームを設定出来ているか。また仕様にあわせて出力しているか。
・パーマリンクとは、URLのこと。仕様にあわせて設定する。
お客さんとどういうページが必要かスプレッドシートでまとめる。パーマリンクをその中。自分出つけてもいいけど、迷ったら相談しても良い。
・管理者ユーザー以外に投稿ユーザーを用意しているか。
わーどプレスの左側にユーザを作れる。管理者ユーザー・投稿者ユーザーを入れておく。
・allin one　SEOのプラグインを入れてあげる。
・メールアドレスで、自分のアドレスで確認して、動作確認する。その後、お客様用のメールアドレスに変更をしておく！！
・traget =blankをつかって、外部サイトに新しくバスを都とばす場合。（aタグに入れるだけ。）

・ぐーぐるサービスについて！！！　グーグルアナリティクスの開設。
サイトは何人きたか分かる。
ユーザーがどのページを見るか。
タグを入れてあげると、データを取れる。
アナリティクスで、どこの国・地域・性別・年代ががどんな人がみたのかが分かる。
→タグをscriptで入れる。bodyやheadに入れる。

アカウント作成して、認証する。（認証方法を）

グーグルサービスはいろいろある。
サービスたくさんタグを使うと大変になる。
タグマネージャーを使う！！！！！、複数のタグを管理してくれる。
・連携するやり方を覚えないといけない。

グーグルサーチコンソールについて！
グーグルがいつ来るのか分かる。自分のサイトにグーグルにきてくださいとお願いすることができる。
そのため、サイト公開して呼べる。（最初は、来てくれない。公開したばかりだから。）